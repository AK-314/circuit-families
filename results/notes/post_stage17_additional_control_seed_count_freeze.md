# Post-Stage-17 additional-control seed-count freeze

## Decision

Stage 18 will run no additional control seeds:

```text
second no-generalisation seed: not executable
additional random-label seeds: 0
```

The five required genuine-task main seeds remain `0, 1, 2, 3, 4`. The existing
random-label seed remains the sole random-label control. Stage 15 remains
unavailable; it is not recoded as an empty family and no replacement control is
introduced.

## Prospective resource basis

The decision uses only evidence available before Stage 18 scientific execution:

- the committed Stage 12 compute projection;
- the post-Stage-17 storage and runtime projection;
- 1,511,969,955,840 available bytes recorded at the concurrency freeze;
- the frozen 12-worker, one-thread production layout;
- the mandatory five-main-seed workload; and
- the project schedule.

The required definitive and independent-reproduction workload projects to
459,775,077,926 bytes before incidental filesystem overhead, approximately
1,672,514 raw files per pass, and 12--16 continuous operational days before
fresh main-seed training. An additional random-label seed would add 126
fidelity-by-distinctness cells per pass and would compete with mandatory
main-seed completion for compute, storage, archive, hashing and schedule
headroom.

No main-seed family result, transfer result, anticipated control direction or
publication preference was used. At the time of this decision, Stage 18 had not
produced training or circuit-analysis output.

## Frozen consequence

Main-seed completion has priority. Stage 18 must record:

```text
additional random-label seeds: 0
reason: resources prioritised for the five required main seeds
```

This decision does not alter the primary seed list, reserve-seed replacement
rule, checkpoint grid, sensitivity surface, search budgets, transfer rules or
production concurrency.
