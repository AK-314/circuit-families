# Stage 15 outcome

Unavailable under the frozen protocol

## Reason

Stage 13 selected no qualifying no-generalisation fraction.

## Actions not taken

* no replacement fraction selected;
* no additional candidate trained;
* no masking run;
* no sparse search;
* no circuit family generated;
* no transfer analysis;
* no sensitivity analysis.

## Scientific consequence

The project does not contain a matched no-generalisation circuit-family control. Comparisons intended to isolate generalisation from reduced-data memorisation are therefore unavailable. This limits causal attribution but does not invalidate the genuine-task analyses or the independent random-label control.

## Interpretation guard

The absence of a Stage 15 result must not be interpreted as evidence that the genuine-task circuit-family pattern is caused specifically by generalisation. The random-label control rules out some simpler explanations, but it does not replace the missing matched no-generalisation comparison.

## Later-stage handling

* later stages must record Stage 15 comparisons as unavailable;
* missing Stage 15 results must not be imputed;
* figures must not display a substitute no-generalisation condition;
* matched comparisons requiring Stage 15 must be marked undefined;
* Stage 16 may proceed because it concerns genuine-task functional transfer.

An unavailable control is not an executed control with an empty family. Stage 15 family size and transfer-group count are undefined, not zero.
